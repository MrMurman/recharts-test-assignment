{
  "name": "line-chart-width-xaxis-padding",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "main": "src/index.tsx",
  "dependencies": {
    "@babel/core": "^7.16.0",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "react-scripts": "4.0.0",
    "react-smooth": "4.0.4",
    "recharts": "2.15.0"
  },
  "devDependencies": {
    "@types/react": "17.0.0",
    "@types/react-dom": "17.0.0",
    "typescript": "4.1.3",
    "gh-pages": "^6.3.0"
  },
  "scripts": {
    "start": "rm -rf tsconfig.json && react-scripts --openssl-legacy-provider start",
    "build": "rm -rf tsconfig.json && react-scripts --openssl-legacy-provider build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "yarn build",
    "deploy": "rm -rf tsconfig.json && react-scripts --openssl-legacy-provider && gh-pages -d build"
  },
  "resolutions": {
    "caniuse-lite": "1.0.30001632"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "homepage": "https://MrMurman.github.io/recharts-test-assignment",
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
